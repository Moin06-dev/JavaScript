const { count } = require("console");

const name = "moinkhan"
const myCount = 50
console.log(name +  myCount + " the merged");

// new way 

console.log(`hey buddy my name is ${name} and my count is ${count}`);